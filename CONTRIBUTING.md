# コントリビューションガイド

本プロジェクトへの参加方法を説明します。ジュニアメンバーの方は上から順に読んでください。

---

## はじめての方へ

### 最短導線

- `README.md` で全体像を確認する。
- `.github/copilot-instructions.md` を読み、GitHub Copilot のルールを確認する。
- `.github/instructions/` 配下の手順書を参照して、最初の 1 機能を実装する。

1. **仕様を先に読む**: `docs/` 以下のドキュメントを把握してから実装を始める（整備中の場合は Issue で確認する）
2. **不明点は Issue で質問する**: コードを書く前に仕様を確認する。推測で実装しない
3. **AI は道具として使う**: GitHub Copilot が生成したコードは必ず自分で理解・確認してから取り込む
4. **小さな変更単位で出す**: 1 変更 = 1 機能/修正。大きな変更は分割する

---

## 開発環境のセットアップ

`README.md` の「クイックスタート」手順を参照してください。

> 前提条件: Python 3.12 以上・SQL Server が起動済みであること。`.env.example` をコピーして `.env` を作成し、接続情報を設定してください。

---

## 開発フロー

```text
Issue 確認 → 設計（必要時）→ 実装 → テスト → 自己レビュー → コードレビュー依頼 → チームレビュー → チェックイン
```

### ステップ詳細

#### 1. Issue を確認する

- 担当する Issue の要件と受け入れ条件を確認する
- 不明点があれば Issue にコメントして質問する（推測しない）

#### 2. 設計する（必要な場合）

- 新規 API エンドポイント追加・データモデル変更・新規ライブラリ導入がある場合は **実装前** に ADR を作成する
- 設計レビューが必要な場合はチームに依頼する

#### 3. 実装する

- コーディング規約（後述）を遵守する
- 仕様が不明な場合は `docs/` を参照し、ない場合は Issue で質問する

#### 4. テストを書く・実行する

```bash
pytest tests/ -v
```

新規ロジックには正常系・異常系・境界値テストが必須です。

#### 5. 自己レビューをする

```bash
mypy app/ --strict     # 型エラーがないか確認
ruff check app/ tests/ # Lint エラーがないか確認
ruff format app/ tests/ --check # フォーマット確認
pytest tests/ -v       # テストが通っているか確認
```

レビュー依頼前に Copilot の `/review` プロンプトで自己チェックすることを推奨します。

#### 6. コードレビューを依頼する

- `.github/instructions/common/review.instructions.md` のチェックリストを確認する
- `docs/` の更新が必要な場合は含める
- レビュアーを最低 1 名アサインする

---

## レビュー依頼（チェックイン）サイズ制限

> **理由**: 8 名並行開発で巨大な差分を出すと、レビュー品質の低下・コンフリクト多発・手戻りリスクが増大する。

| ルール | 基準 |
| --- | --- |
| **1 レビュー依頼 ≤ 400 行差分**（テスト・自動生成ファイル除く） | 400 行を超える場合は分割を検討する |
| **1 レビュー依頼 = 1 機能 / 1 修正** | 複数の無関係な変更を混ぜない |
| **分割できない場合** | レビュー依頼時にコメントで理由を記載し、レビュアーに事前に共有する |

### 分割の指針

- Router + Service + Repository + テスト = 1 レビュー依頼（1 エンドポイント単位）
- スキーマ変更とロジック変更は別レビュー依頼
- `common/` への追加と各機能モジュールでの利用は別レビュー依頼にできる場合は分ける

---

## `common/` への追加プロセス

> **理由**: `common/` は全機能から依存される共有モジュールであり、安易な追加は結合度の増大と保守コストの増大を招く。

### `common/` に新規ファイルを追加する条件

以下の **いずれか** を満たすこと:

1. **ADR を作成する**: 破壊的変更・新しいパターンの導入に該当する場合（`docs/decisions/` に記録する）
2. **チーム合意を得る**: ADR が不要な軽微な追加の場合、チーム MTG または Slack で合意を取り、Issue にその旨を記録する

### `common/` 変更時のレビュー要件

| 変更種別 | レビュー要件 |
| --- | --- |
| 新規ファイル追加 | TL レビュー必須 + ADR またはチーム合意の証跡 |
| 既存ファイルの破壊的変更 | TL レビュー必須 + 影響を受ける全ドメイン担当の確認 |
| 既存ファイルの非破壊的修正 | TL レビュー必須 |

### `core/` および特殊モジュールのレビュー要件

| 対象 | ルール |
| --- | --- |
| `app/core/` への変更 | TL レビュー必須 |
| 2 つ以上のドメインにまたがる変更 | 全関連ドメイン担当のレビュー必須 |
| `app/common/` の重要モジュール変更 | TL レビュー必須 + 全チームへの周知（Slack 等） |

---

## コーディング規約

詳細は `.github/copilot-instructions.md` および各 `.github/instructions/` ファイルに定義されています。主なルール:

| カテゴリ   | 規約                                                                      |
| ---------- | ------------------------------------------------------------------------- |
| Python     | `Any` 禁止・全関数の戻り値型を明示・Result パターンを使用                 |
| FastAPI    | Router にビジネスロジックを書かない・Pydantic でバリデーション            |
| SQLAlchemy | N+1 禁止・全クエリに `delete_flg == 0` フィルタ・`OrganizationScope` 必須 |
| テスト     | pytest・Arrange/Act/Assert 構造・テストデータに個人情報を含めない         |

---

## AI 駆動開発（Copilot の使い方）

Copilot Chat の `/` メニューから専用プロンプトを呼び出せます（`.github/prompts/` 配下に定義）。

- ルール設定: `.github/copilot-instructions.md`
- タスク別手順書: `.github/instructions/`
- 専用エージェント: `.github/agents/`
- 再利用可能プロンプト: `.github/prompts/`
- AI 専門知識モジュール: `.github/skills/`

---

## 禁止事項

| 禁止事項                                   | 理由                                          |
| ------------------------------------------ | --------------------------------------------- |
| テストデータに個人情報を含める             | プライバシー保護                              |
| シークレット・API キーをコードに書く       | セキュリティ（`.env` に記述すること）         |
| OSS のソースコードをコピー・模倣する       | GPL・MPL 等のコピーレフトライセンス感染リスク |
| テストをパスさせるためだけにコードを変える | 根本原因を修正し、チームに報告する            |
| 推測で仕様を実装する                       | 不明点は必ず Issue で確認する                 |

---

## 仕様が見つからない場合

1. `docs/` 以下のドキュメントを検索する
2. 関連する Issue を検索する
3. 見つからない場合は **新しい Issue を作成して質問する**（コードを書いて後で修正するより効率的）
